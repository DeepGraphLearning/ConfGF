import setuptools

if __name__ == "__main__":
    setuptools.setup(
        name="confgf",
        version="0.1.0",
        packages=setuptools.find_packages(include=["confgf"]),
        python_requires=">=3.5",
    )